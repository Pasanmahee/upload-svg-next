# Message Auto-Dismiss Fix

Updated the Game Settings admin notification so success messages auto-hide after 3.5 seconds and error messages auto-hide after 6.5 seconds.

Also added a close button so the admin can dismiss the message immediately.

Changed files:

- `app/game-settings/page.tsx`
- `app/globals.css`
